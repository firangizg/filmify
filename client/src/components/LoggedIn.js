import React from 'react';

// LoggedIn status page
const LoggedIn = () => {
  // Returns a simple message to let users know they are logged in successfully
  return (
    <div>
      You're logged in!
    </div>
  );
};

export default LoggedIn;